#!/bin/sh

if [ ! -f /tmp/64m.vfat ]; then
	umass-pre
fi

modprobe -r g_mass_storage

grep -q "/mnt" /proc/mounts

if [ $? -ne 0 ]; then
	mount -t vfat /tmp/64m.vfat /mnt
fi

if [ -x ${HOME}/.umass-mnt-hook ]; then
	sh ${HOME}/.umass-mnt-hook
fi

